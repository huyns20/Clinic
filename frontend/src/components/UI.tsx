import {Search,Plus} from 'lucide-react';
export function PageTitle({title,sub,action,onAction}:{title:string,sub?:string,action?:string,onAction?:()=>void}){return <div className="page-title"><div><h1>{title}</h1>{sub&&<p>{sub}</p>}</div>{action&&<button className="primary" onClick={onAction}><Plus size={17}/>{action}</button>}</div>}
export function SearchBar({placeholder='Tìm kiếm...'}:{placeholder?:string}){return <div className="search"><Search size={17}/><input placeholder={placeholder}/></div>}
export function Table({heads,rows,statusCol=-1}:{heads:string[],rows:any[][],statusCol?:number}){return <div className="table-wrap"><table><thead><tr>{heads.map(x=><th>{x}</th>)}<th>Thao tác</th></tr></thead><tbody>{rows.map((r,i)=><tr key={i}>{r.map((x,j)=><td>{j===statusCol?<span className={'badge '+(String(x).includes('Đã')?'green':'amber')}>{x}</span>:x}</td>)}<td><button className="link">Xem</button><button className="link">Sửa</button></td></tr>)}</tbody></table></div>}
export function Modal({title,onClose,children}:{title:string,onClose:()=>void,children:any}){return <div className="overlay"><div className="modal"><div className="modal-head"><h2>{title}</h2><button onClick={onClose}>×</button></div>{children}</div></div>}
